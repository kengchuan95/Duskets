using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class DusketBehaviour : WeaponScript
{
    private System.Random rand = new System.Random();

    void Awake()
    {
        //currentBullets = 1;
        //maxBulletDamage = 21; //max damage + 1 for the upper bounds
        //reloadDelay = 1f;
    }
}
